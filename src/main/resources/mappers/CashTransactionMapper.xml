<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.demo.brokerage.mapper.CashTransactionMapper">

    <select id="selectTransactionsByAccount" resultType="com.kh.demo.brokerage.dto.CashTransactionDto">
        SELECT ct.cash_transaction_id,
               ct.account_id,
               ct.transaction_type,
               ct.amount,
               ct.balance_after,
               ct.memo,
               ct.transaction_at
        FROM cash_transaction ct
        <where>
            ct.account_id = #{accountId}
            <if test="from != null">
                AND ct.transaction_at &gt;= #{from}
            </if>
            <if test="to != null">
                AND ct.transaction_at &lt; DATE_ADD(#{to}, INTERVAL 1 DAY)
            </if>
        </where>
        ORDER BY ct.transaction_at DESC
    </select>

</mapper>
