<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- MemberMapper 인터페이스와 연결 -->
<mapper namespace="com.kh.demo.member.mapper.MemberMapper">

    <!-- 회원가입 정보를 member 테이블에 저장 -->
    <insert id="insertMember" parameterType="MemberDto">
        INSERT INTO member (
            member_id,
            member_pwd,
            member_name,
            nickname,
            email,
            profile
        ) VALUES (
        #{memberId},
        #{memberPwd},
        #{memberName},
        #{nickname},
        #{email},
        #{profile}
        )
    </insert>

    <!-- 새 회원의 프로필 설정 기본값을 저장 -->
    <insert id="insertDefaultSettings">
        INSERT INTO settings (
        member_id,
        is_stock_public,
        is_word_tooltip
        ) VALUES (
        #{memberId},
        FALSE,
        TRUE
        )
    </insert>

    <!-- 같은 아이디를 가진 회원 수를 조회 -->
    <select id="countByMemberId" resultType="int">
        SELECT COUNT(*)
        FROM member
        WHERE member_id = #{memberId}
    </select>

    <!-- 같은 닉네임을 가진 회원 수를 조회 -->
    <select id="countByNickname" resultType="int">
        SELECT COUNT(*)
        FROM member
        WHERE nickname = #{nickname}
    </select>

    <!-- 같은 이메일을 가진 회원 수를 조회 -->
    <select id="countByEmail" resultType="int">
        SELECT COUNT(*)
        FROM member
        WHERE email = #{email}
    </select>

    <!-- 새 코드를 발급하기 전에 가입에 연결되지 않은 이전 인증 기록을 모두 무효화 -->
    <update id="expireEmailVerificationCodes">
        UPDATE email_verification
        SET is_verified = FALSE,
            verified_at = NULL,
            expired_at = CURRENT_TIMESTAMP
        WHERE email = #{email}
          AND member_id IS NULL
    </update>

    <!-- 6자리 이메일 인증 코드를 5분 동안 유효하게 저장 -->
    <insert id="insertEmailVerification">
        INSERT INTO email_verification (
            email,
            code,
            expired_at
        ) VALUES (
            #{email},
            #{code},
            DATE_ADD(CURRENT_TIMESTAMP, INTERVAL 5 MINUTE)
        )
    </insert>

    <!-- 유효한 이메일 인증 코드와 일치하면 인증 완료로 변경 -->
    <update id="verifyEmailCode">
        UPDATE email_verification
        SET is_verified = TRUE,
            verified_at = CURRENT_TIMESTAMP
        WHERE email = #{email}
          AND code = #{code}
          AND is_verified = FALSE
          AND expired_at &gt; CURRENT_TIMESTAMP
        ORDER BY verification_id DESC
        LIMIT 1
    </update>

    <!-- 아직 회원과 연결되지 않은 인증 완료 기록을 조회 -->
    <select id="countVerifiedEmail" resultType="int">
        SELECT COUNT(*)
        FROM email_verification
        WHERE email = #{email}
          AND is_verified = TRUE
          AND member_id IS NULL
    </select>

    <!-- 가장 최근 인증 완료 기록을 가입한 회원과 연결 -->
    <update id="linkVerifiedEmailToMember">
        UPDATE email_verification
        SET member_id = #{memberId}
        WHERE email = #{email}
          AND is_verified = TRUE
          AND member_id IS NULL
        ORDER BY verification_id DESC
        LIMIT 1
    </update>

    <!-- 로그인에 사용할 회원 정보를 아이디로 조회 -->
    <select id="selectByMemberId" resultType="MemberDto">
        SELECT m.member_id,
               m.member_pwd,
               m.member_name,
               m.nickname,
               m.email,
               COALESCE(NULLIF(m.profile, ''), '/images/common_member.png') AS profile,
               m.member_role,
               m.member_status,
               m.create_at,
               DATE_FORMAT(m.create_at, '%Y-%m-%d %H:%i') AS create_at_str,
               COALESCE(s.is_stock_public, FALSE) AS stock_public,
               COALESCE(s.is_word_tooltip, TRUE) AS word_tooltip
        FROM member m
        LEFT JOIN settings s
               ON s.member_id = m.member_id
        WHERE m.member_id = #{memberId}
    </select>

    <!-- 현재 회원을 제외하고 같은 닉네임 수를 조회 -->
    <select id="countByNicknameExceptMember" resultType="int">
        SELECT COUNT(*)
        FROM member
        WHERE nickname = #{nickname}
        AND member_id != #{memberId}
    </select>

    <!-- 이름·닉네임·이메일·프로필 이미지와 입력된 경우 새 비밀번호를 수정 -->
    <update id="updateMemberProfile" parameterType="MemberDto">
        UPDATE member
        SET member_name = #{memberName},
            nickname = #{nickname},
            email = #{email},
            profile = #{profile}
            <if test="memberPwd != null and memberPwd != ''">
                , member_pwd = #{memberPwd}
            </if>
        WHERE member_id = #{memberId}
    </update>

    <!-- 프로필 이미지 삭제 시 기본 이미지 경로로 변경 -->
    <update id="updateProfileImage">
        UPDATE member
        SET profile = #{profile}
        WHERE member_id = #{memberId}
    </update>

    <!-- 설정이 있으면 수정하고 없으면 새로 저장 -->
    <insert id="upsertSettings" parameterType="MemberDto">
        INSERT INTO settings (
        member_id,
        is_stock_public,
        is_word_tooltip
        ) VALUES (
        #{memberId},
        #{stockPublic},
        #{wordTooltip}
        )
        ON DUPLICATE KEY UPDATE
        is_stock_public = VALUES(is_stock_public),
        is_word_tooltip = VALUES(is_word_tooltip)
    </insert>

    <!-- 회원의 계좌 연동을 모두 해제 (account/holding/trade는 증권사 소유라 보존됨) -->
    <delete id="deleteAccountLinksByMemberId">
        DELETE FROM account_link
        WHERE member_id = #{memberId}
    </delete>

    <!-- 아직 연동된 증권사 계좌가 하나도 없는 회원 목록 (데모 데이터 생성기 전용) -->
    <select id="selectMembersWithoutAccount" resultType="MemberDto">
        SELECT m.member_id, m.member_name
        FROM member m
        LEFT JOIN account_link al ON al.member_id = m.member_id
        WHERE m.member_role = 'USER' AND al.link_id IS NULL
        ORDER BY m.member_id
    </select>

    <!-- 회원 기본 정보를 삭제 -->
    <delete id="deleteMemberById">
        DELETE FROM member
        WHERE member_id = #{memberId}
    </delete>

</mapper>
