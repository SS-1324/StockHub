<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.demo.goal.mapper.GoalMapper">

    <insert id="insertGoal" parameterType="com.kh.demo.goal.dto.GoalDto"
            useGeneratedKeys="true" keyProperty="goalId">
        INSERT INTO goal(member_id, goal_type, title, target_value)
        VALUES(#{memberId}, #{goalType}, #{title}, #{targetValue})
    </insert>

    <select id="selectLatestGoalByMember" resultType="com.kh.demo.goal.dto.GoalDto">
        SELECT goal_id, member_id, goal_type, title, target_value, create_at
        FROM goal
        WHERE member_id = #{memberId}
        ORDER BY create_at DESC
        LIMIT 1
    </select>

</mapper>
