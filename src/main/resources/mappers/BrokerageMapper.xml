<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.demo.brokerage.mapper.BrokerageMapper">

    <select id="selectAllBrokerages" resultType="com.kh.demo.brokerage.dto.BrokerageDto">
        SELECT brokerage_id, brokerage_name, fee_rate, create_at
        FROM brokerage
        ORDER BY brokerage_id
    </select>

    <select id="selectBrokerageById" parameterType="long" resultType="com.kh.demo.brokerage.dto.BrokerageDto">
        SELECT brokerage_id, brokerage_name, fee_rate, create_at
        FROM brokerage
        WHERE brokerage_id = #{brokerageId}
    </select>

</mapper>
