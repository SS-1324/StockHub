<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.demo.brokerage.mapper.StockPriceHistoryMapper">

    <select id="selectHistoryByStock" parameterType="string" resultType="com.kh.demo.brokerage.dto.StockPriceHistoryDto">
        SELECT history_id, stock_code, price, recorded_at
        FROM stock_price_history
        WHERE stock_code = #{stockCode}
        ORDER BY recorded_at ASC
    </select>

    <select id="selectLatestAsOf" resultType="com.kh.demo.brokerage.dto.StockPriceHistoryDto">
        SELECT history_id, stock_code, price, recorded_at
        FROM stock_price_history
        WHERE stock_code = #{stockCode}
          AND recorded_at &lt;= #{asOf}
        ORDER BY recorded_at DESC
        LIMIT 1
    </select>

    <delete id="deleteAll">
        DELETE FROM stock_price_history
    </delete>

    <insert id="insertBatch" parameterType="list">
        INSERT INTO stock_price_history(stock_code, price, recorded_at)
        VALUES
        <foreach collection="list" item="h" separator=",">
            (#{h.stockCode}, #{h.price}, #{h.recordedAt})
        </foreach>
    </insert>

</mapper>
